package poo.exercicios.interfaces;

public class helper {
/*	Atividade 1: 
		Crie uma interface chamada "Shape" com métodos como "calculateArea" e "calculatePerimeter". 
		Em seguida, crie duas classes concretas, como "Circle" e "Rectangle",
		que implementam essa interface e fornecem a implementação específica para os cálculos de área e perímetro.

	Atividade 2: 
		Crie uma interface chamada "Database" com métodos como "connect", "disconnect", "query" e "execute". 
		Em seguida, crie duas classes concretas, como "MySQLDatabase" e "PostgreSQLDatabase", 
		que implementam essa interface e fornecem a implementação específica para cada banco de dados. 
		Obs: essas implementações devem ser uma saída em texto apenas para exemplificar os métodos. 
Atividade 3: 
		Crie uma interface chamada "Animal" com métodos como "eat", "sleep" e "makeSound".
		Em seguida, crie três classes concretas, como "Dog", "Cat" e "Bird", 
		que implementam essa interface e fornecem a implementação específica para cada animal.
		Obs: essas implementações devem ser uma saída em texto apenas para exemplificar os métodos.
*/

}
